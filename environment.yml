name: crispr_tool_env
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.12
  - django
  - pandas
  - biopython
  - requests
  - openpyxl
  - psycopg2 # PostgreSQL
  - pip
  - pytest # tests
  - pytest-django # Django-Tests
